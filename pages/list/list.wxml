<view wx:for="{{articles}}" wx:key="{{item.id}}" class="list border-bottom border-shadow p-y-min text-default" bindtap="onTabNode" data-id="{{item.id}}">
    <view class='list-left'>
      <h3>{{item.attributes.title}}</h3>
      <view class='list-info text-light'>
        {{item.attributes.field_author}}
        <span>{{item.attributes.changed}}</span>
      </view>
    </view>
    <view class='list-right'>
      <image wx:if="{{imagesList[item.relationships.field_image.data.id]}}" src="{{imagesList[item.relationships.field_image.data.id]}}"></image>
      <image wx:if="{{!imagesList[item.relationships.field_image.data.id]}}" src="/assets/images/thumb.jpg"></image>
    </view>
  </view>